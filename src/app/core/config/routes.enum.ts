export enum ROUTES {
  ABOUT = 'about',
  ALL = '**',
  AUTH = 'auth',
  TEAMS = 'teams',
  EMPTY = '',
  FORBIDDEN = 'forbidden',
  // ID = ':id',
  LOGIN = 'login',
  NOT_FOUND = 'not-found',
  PROFILE = 'profile',
  USERS = 'users'
}
